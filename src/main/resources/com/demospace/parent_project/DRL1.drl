package com.demospace.parent_project;

rule "rule 1"
	ruleflow-group "ruleflow-group1"
	dialect "mvel"
	when
		testDO : TestDO(resultValue.toString().equals("Asdf") );
		
	then
		testDO.setResultValue( "one" );
end